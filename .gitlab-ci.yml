image: python:3.6

pages:
  stage: deploy
  script:
  - python3 -m pip install -U pip
  - pip install pipenv
  - pipenv install
  - make gitlab
  - mv output public
  artifacts:
    paths:
    - public/
  only:
  - master
