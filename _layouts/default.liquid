<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ page.title }}</title>
    </head>
    <body>
    <!-- A web hipster told me to centralize with the following top div style -->
    <div style="max-width: 920px; margin: auto; padding: 20px; box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.2);">
      <h2>{{ page.title }}</h2>
      {{ page.content }}
    </div>
  </body>
</html>
